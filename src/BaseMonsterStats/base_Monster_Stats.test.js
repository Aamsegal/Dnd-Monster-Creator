import React from 'react';
import { shallow } from 'enzyme';
import ReactDOM from 'react-dom';
import BaseMonsterStats from './baseMonsterStats';

describe('Base Monster Stats component tests', () => {
   it('renders without crashing', () => {
      <BaseMonsterStats />;
    });
});