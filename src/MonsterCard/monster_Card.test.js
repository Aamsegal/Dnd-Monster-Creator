import React from 'react';
import { shallow } from 'enzyme';
import ReactDOM from 'react-dom';
import MonsterCard from './MonsterCard';

describe('Monster Card component tests', () => {
   it('renders without crashing', () => {
      <MonsterCard />;
    });
});